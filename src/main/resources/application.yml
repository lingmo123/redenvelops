server:
  port: 8081
#数据库配置
spring:
  datasource:
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/RedEnvelop?useUnicode=true&characterEncoding=utf8
#配置日志，查看执行的sql语句
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

    #redis配置
#  redis:
#    host: 127.0.0.1
#    port: 6379
##    lettuce:
##      pool:
##        max-wait: 30000 #连接池最大阻塞等待时间（使用负值表示没有限制）
##        max-active: 100 #连接池最大连接数（使用负值表示没有限制）
##        max-idle: 20 #连接池中最大空闲连接
##        min-idle: 0 #连接池中最小空闲连接

